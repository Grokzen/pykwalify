type: seq
sequence:
  - include: fooone

schema;foothree:
  type: seq
  sequence:
    - type: str

schema;footwo:
  type: seq
  sequence:
    - include: foothree

schema;fooone:
  type: seq
  sequence:
    - include: footwo
