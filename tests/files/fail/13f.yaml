data:
  streams:
    sampleRateMultiple: 1
schema:
  type: map
  mapping:
    streams:
      type: map
      range:
        min: 2
        max: 3
      mapping:
        sampleRateMultiple:
          type: int
          required: True
errors:
  - "Type 'map' has size of '1', less than min limit '2'. Path: '/streams'"
