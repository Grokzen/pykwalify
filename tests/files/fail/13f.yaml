data:
  streams:
    sampleRateMultiple: 1
schema:
  type: map
  mapping:
    streams:
      type: map
      range:
        min: 2
        max: 3
      mapping:
        sampleRateMultiple:
          type: int
          required: True
errors:
  - "map.range.toosmall : 2 > 1 : /streams"
