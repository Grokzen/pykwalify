data:
  mic:
  - input
    foo
  - output
    bar
schema:
  type: map
  matching-rule: "any"
  mapping:
    regex;mi.+:
      type: seq
      sequence:
        - type: str
parsed-struct:
  type: map
  matching-rule: "any"
  mapping:
    regex;mi.+:
      type: seq
      sequence:
        - type: str
